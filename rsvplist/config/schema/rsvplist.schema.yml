rsvplist.settings:
  type: config_object
  label: 'RSVP list content type settings'   
  mapping:
    allowed_types:
      type: sequence
      label: 'content types rsvp Form can display on'
      sequence: 
        type: string
        label: 'Content type'